prepend head
	link(rel="stylesheet" href="../components/ErrorPage/style.sass")

mixin errPage(errCode, errStatus)
	.page__error
		.page__errorwrapper
			.page__errorcode
				span #{errCode}
			.page__errordesc #{errStatus}
			+extraLink("./index.pug", "link__back", "вернуться")