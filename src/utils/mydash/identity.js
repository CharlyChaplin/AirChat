/**
 * Функция возвращает то, что в неё пришло.
 * 
 * @param {any} value любой аргумент
 * @returns Возвращает то, что пришло в функцию
 */

function identity(value) {
	return value;
}


export default identity;